{
  "name": "@gs0428/toast",
  "version": "0.0.7",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build.config.ts && bun run build:types",
    "build:types": "tsc --project tsconfig.json --emitDeclarationOnly",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "zustand": "^4.0.0"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.12",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
